{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "CMake: Build Release",
            "type": "shell",
            "command": "cmake --build build --config Release",
            "group": "build",
            "presentation": {
                "reveal": "always"
            }
        }
    ]
}