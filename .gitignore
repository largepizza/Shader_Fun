# ── Build output ──────────────────────────────────────────────────────────────
build/
build-linux/
out/
dist/
build-win-release/
build-linux-release/
.logs/


# ── Compiled shaders (generated at build time from shaders/) ─────────────────
*.spv

# ── VS Code local state (workspace config is kept, local state is not) ────────
.vscode/*.log

# ── OS artifacts ─────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Compiler artifacts ────────────────────────────────────────────────────────
*.obj
*.ilk
*.pdb
*.exe
*.lib
*.dll

# - Debug
.renderDoc_captures/
.claude
.plans

# ── GPU capture / benchmark scratch (kept in-repo, never committed) ───────────
benchmarks/

# ── Python tooling (tools/perf_analysis, etc.) ─────────────────────────────────
.venv/
__pycache__/
*.pyc